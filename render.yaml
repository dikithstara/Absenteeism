# services:
#   - type: web
#     name: absenteeism-backend
#     env: python
#     buildCommand: pip install -r requirements.txt
#     startCommand: uvicorn backend.app:app --host 0.0.0.0 --port 8000
#     envVars:
#       - key: PYTHONPATH
#         value: backend
#     autoDeploy: true


services:
  - type: web
    name: absenteeism-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0